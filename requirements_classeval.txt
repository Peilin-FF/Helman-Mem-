# What ClassEval's classes and their tests import (the grader runs them with the sigma env's python): on top of
# requirements_qwen3.txt, then `python -m pipeline.classeval setup` for the pinned data and nltk's corpora.
# numpy stays at requirements_qwen3.txt's 1.26.4 (the benchmark's gold calls np.mat, removed in NumPy 2.0).
gensim>=4.3,<4.4
PyPDF2~=3.0.1
reportlab~=4.0
python-docx>=1.1
openpyxl>=3.1
beautifulsoup4>=4.12
Pillow>=10.0
nltk>=3.8
pandas>=2.0
