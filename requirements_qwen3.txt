# Requirements for the Qwen3 center models (Qwen3-0.6B / 4B-Instruct / 8B).
# This is the MAIN environment: transformers 4.56.2 recognizes qwen3 and also runs the
# vLLM peer-generation data pipeline. Use this for everything EXCEPT loading Qwen3.5.
#
#   pip install -r requirements_qwen3.txt
#   (vLLM line: install separately with --no-deps, see note below)

torch==2.6.0
torchvision==0.21.0
transformers==4.56.2
huggingface-hub==0.35.3
tokenizers==0.22.2
numpy==1.26.4

# --- training / adapters ---
peft==0.19.1
accelerate==1.13.0
datasets==4.8.5
safetensors==0.7.0
sentencepiece==0.2.1
deepspeed==0.19.1

# --- inference (vLLM installed with --no-deps; deps pinned explicitly) ---
# pip install vllm==0.8.5 --no-deps
vllm==0.8.5
xformers==0.0.29.post2
triton==3.2.0
ray==2.55.1
outlines==0.1.11
openai==2.24.0
pyarrow==24.0.0

# --- answer grading (no torch/transformers deps; safe to bump independently) ---
math-verify==0.9.0
latex2sympy2_extended==1.11.0
antlr4-python3-runtime==4.13.2

# --- utilities ---
wandb==0.27.0
hjson==3.1.0
einops==0.8.0
pydantic==2.13.4
tqdm
ninja
psutil
py-cpuinfo
msgpack
packaging
pytest
