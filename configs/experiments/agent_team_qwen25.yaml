# The lead-worker team with Qwen2.5-7B as the lead (docs/experiments/agent_team.md): the same pool of workers and the same sub-tasks
# (their reports are generated once and shared); the lead's own answers, its check of every report and the record's addresses are
# Qwen2.5-7B's.
base: agent_team.yaml
name: agent_team_qwen25
central: [qwen25]
team:
  checker: qwen25                 # the model behind the lead's check: the lead's own
