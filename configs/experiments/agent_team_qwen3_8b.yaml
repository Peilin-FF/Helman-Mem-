# The lead-worker team with Qwen3-8B as the lead (docs/experiments/agent_team.md): the same pool of workers and the same sub-tasks
# (their reports are generated once and shared), the lead's own answers, its check of every report and the record's addresses are
# Qwen3-8B's.
base: agent_team.yaml
name: agent_team_qwen3_8b
central: [qwen3_8b]
team:
  checker: qwen3_8b                 # the model behind the lead's check: the lead's own
