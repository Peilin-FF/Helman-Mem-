# A worker of the research team (pipeline.team): meta-llama/Llama-3.1-8B-Instruct answering sub-questions, BM25 top-3 over every paragraph of the split (an open index).
model: llama31                      # a registered model (configs/models/)
tool: global3                       # the view of the stream it answers from (the stream's `views`)
